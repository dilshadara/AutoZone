import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen w-full flex justify-center mt-20'>
            <button class="btn loading btn-accent font-bold">loading</button>
        </div>
    );
};

export default Loading;